// backend/services/memoryOptimizedSupervisorSync.js
// Memory-optimized supervisor sync service with connection limits and cleanup

import memoryMonitor from './memoryMonitor.js';

class MemoryOptimizedSupervisorSync {
  constructor() {
    this.connections = new Map(); // Simplified connection tracking
    this.displayState = {
      alerts: [],
      lastUpdated: new Date().toISOString()
    };
    
    // Aggressive connection limits for memory conservation
    this.MAX_CONNECTIONS = 20; // Total connections limit
    this.MAX_CONNECTIONS_PER_IP = 3; // Per IP limit
    this.CONNECTION_TIMEOUT = 300000; // 5 minutes
    this.CLEANUP_INTERVAL = 60000; // 1 minute cleanup
    
    // Connection tracking
    this.ipConnections = new Map();
    this.lastActivity = new Map();
    
    // Memory optimization
    this.memoryPressureMode = false;
    this.maxStateHistory = 5; // Limit state history
    this.stateHistory = [];
    
    // Start cleanup
    this.startCleanup();
    
    console.log('🔌 Memory-optimized Supervisor Sync initialized');
  }

  // Memory-efficient connection handler
  handleConnection(connectionId, metadata = {}) {
    // Check global connection limit
    if (this.connections.size >= this.MAX_CONNECTIONS) {
      console.warn(`🚫 Connection limit reached: ${this.connections.size}/${this.MAX_CONNECTIONS}`);
      this.evictOldestConnection();
    }
    
    // Check per-IP limit
    const ip = metadata.ip || 'unknown';
    const ipCount = this.ipConnections.get(ip) || 0;
    
    if (ipCount >= this.MAX_CONNECTIONS_PER_IP) {
      console.warn(`🚫 IP connection limit reached for ${ip}: ${ipCount}/${this.MAX_CONNECTIONS_PER_IP}`);
      return false;
    }
    
    // Create lightweight connection object
    const connection = {
      id: connectionId,
      type: metadata.type || 'unknown',
      ip: ip,
      connectedAt: Date.now(),
      lastActivity: Date.now(),
      supervisorId: metadata.supervisorId || null,
      // Minimal data to reduce memory footprint
      messageCount: 0,
      isActive: true
    };\n    
    // Store connection\n    this.connections.set(connectionId, connection);\n    this.ipConnections.set(ip, ipCount + 1);\n    this.lastActivity.set(connectionId, Date.now());\n    \n    console.log(`✅ Connection registered: ${connectionId} (${this.connections.size}/${this.MAX_CONNECTIONS})`);\n    \n    return true;\n  }\n  \n  // Evict oldest connection to free memory\n  evictOldestConnection() {\n    if (this.connections.size === 0) return;\n    \n    let oldestId = null;\n    let oldestTime = Date.now();\n    \n    for (const [id, connection] of this.connections) {\n      if (connection.connectedAt < oldestTime) {\n        oldestTime = connection.connectedAt;\n        oldestId = id;\n      }\n    }\n    \n    if (oldestId) {\n      this.removeConnection(oldestId);\n      console.log(`🗑️ Evicted oldest connection: ${oldestId}`);\n    }\n  }\n  \n  // Memory-efficient message handling\n  handleMessage(connectionId, messageType, data = {}) {\n    const connection = this.connections.get(connectionId);\n    if (!connection) {\n      console.warn(`⚠️ Message from unknown connection: ${connectionId}`);\n      return false;\n    }\n    \n    // Update activity\n    connection.lastActivity = Date.now();\n    connection.messageCount++;\n    this.lastActivity.set(connectionId, Date.now());\n    \n    // Handle message based on type (simplified)\n    switch (messageType) {\n      case 'ping':\n        return this.handlePing(connectionId);\n      case 'update_alerts':\n        return this.handleAlertsUpdate(connectionId, data);\n      case 'acknowledge_alert':\n        return this.handleAlertAcknowledgment(connectionId, data);\n      default:\n        console.log(`📨 Received ${messageType} from ${connectionId}`);\n        return true;\n    }\n  }\n  \n  // Simplified message handlers\n  handlePing(connectionId) {\n    const connection = this.connections.get(connectionId);\n    if (connection) {\n      connection.lastActivity = Date.now();\n      return { type: 'pong', timestamp: new Date().toISOString() };\n    }\n    return false;\n  }\n  \n  handleAlertsUpdate(connectionId, data) {\n    const connection = this.connections.get(connectionId);\n    if (!connection || connection.type !== 'supervisor') {\n      return false;\n    }\n    \n    // Update display state with memory limits\n    if (data.alerts && Array.isArray(data.alerts)) {\n      // Limit alerts to prevent memory bloat\n      this.displayState.alerts = data.alerts.slice(0, 100);\n      this.displayState.lastUpdated = new Date().toISOString();\n      \n      // Store in limited history\n      this.addToStateHistory({\n        type: 'alerts_update',\n        timestamp: Date.now(),\n        alertCount: data.alerts.length,\n        supervisorId: connection.supervisorId\n      });\n      \n      console.log(`🔄 Alerts updated by ${connection.supervisorId}: ${data.alerts.length} alerts`);\n      return true;\n    }\n    \n    return false;\n  }\n  \n  handleAlertAcknowledgment(connectionId, data) {\n    const connection = this.connections.get(connectionId);\n    if (!connection || connection.type !== 'supervisor') {\n      return false;\n    }\n    \n    // Simple acknowledgment tracking (memory-efficient)\n    this.addToStateHistory({\n      type: 'alert_acknowledged',\n      timestamp: Date.now(),\n      alertId: data.alertId,\n      supervisorId: connection.supervisorId\n    });\n    \n    console.log(`✅ Alert ${data.alertId} acknowledged by ${connection.supervisorId}`);\n    return true;\n  }\n  \n  // Add to limited state history\n  addToStateHistory(entry) {\n    this.stateHistory.push(entry);\n    \n    // Trim history to prevent memory buildup\n    if (this.stateHistory.length > this.maxStateHistory) {\n      this.stateHistory.shift();\n    }\n  }\n  \n  // Remove connection and cleanup\n  removeConnection(connectionId) {\n    const connection = this.connections.get(connectionId);\n    if (!connection) return false;\n    \n    // Update IP connection count\n    const ipCount = this.ipConnections.get(connection.ip) || 1;\n    if (ipCount <= 1) {\n      this.ipConnections.delete(connection.ip);\n    } else {\n      this.ipConnections.set(connection.ip, ipCount - 1);\n    }\n    \n    // Remove from all tracking\n    this.connections.delete(connectionId);\n    this.lastActivity.delete(connectionId);\n    \n    console.log(`🔌 Connection removed: ${connectionId} (${this.connections.size} remaining)`);\n    return true;\n  }\n  \n  // Broadcast message to active connections (memory-efficient)\n  broadcast(message, filter = null) {\n    let sent = 0;\n    const messageStr = JSON.stringify(message);\n    \n    // Limit message size to prevent memory issues\n    if (messageStr.length > 10000) {\n      console.warn(`⚠️ Message too large for broadcast: ${messageStr.length} bytes`);\n      return 0;\n    }\n    \n    for (const [id, connection] of this.connections) {\n      if (!connection.isActive) continue;\n      \n      // Apply filter if provided\n      if (filter && !filter(connection)) continue;\n      \n      try {\n        // In a real implementation, this would send via WebSocket\n        // For memory optimization, we just track the intent\n        connection.lastActivity = Date.now();\n        sent++;\n      } catch (error) {\n        console.error(`❌ Failed to send to ${id}:`, error.message);\n        this.removeConnection(id);\n      }\n    }\n    \n    if (sent > 0) {\n      console.log(`📡 Broadcast sent to ${sent} connections`);\n    }\n    \n    return sent;\n  }\n  \n  // Memory pressure handling\n  handleMemoryPressure(type) {\n    console.log(`🧹 Handling memory pressure: ${type}`);\n    \n    if (type === 'emergency') {\n      this.memoryPressureMode = true;\n      \n      // Aggressive cleanup\n      this.performAggressiveCleanup();\n    } else {\n      // Normal cleanup\n      this.performNormalCleanup();\n    }\n  }\n  \n  performAggressiveCleanup() {\n    const before = this.connections.size;\n    \n    // Remove inactive connections immediately\n    const now = Date.now();\n    const toRemove = [];\n    \n    for (const [id, connection] of this.connections) {\n      if (now - connection.lastActivity > 60000) { // 1 minute timeout in emergency\n        toRemove.push(id);\n      }\n    }\n    \n    toRemove.forEach(id => this.removeConnection(id));\n    \n    // Clear state history\n    this.stateHistory = [];\n    \n    // Limit display state\n    if (this.displayState.alerts.length > 20) {\n      this.displayState.alerts = this.displayState.alerts.slice(-20);\n    }\n    \n    console.log(`🚨 Aggressive cleanup: removed ${toRemove.length} connections, ${before - this.connections.size} total cleanup`);\n  }\n  \n  performNormalCleanup() {\n    const now = Date.now();\n    const toRemove = [];\n    \n    // Remove timed out connections\n    for (const [id, connection] of this.connections) {\n      if (now - connection.lastActivity > this.CONNECTION_TIMEOUT) {\n        toRemove.push(id);\n      }\n    }\n    \n    toRemove.forEach(id => this.removeConnection(id));\n    \n    // Trim state history if too large\n    if (this.stateHistory.length > this.maxStateHistory) {\n      this.stateHistory = this.stateHistory.slice(-this.maxStateHistory);\n    }\n    \n    if (toRemove.length > 0) {\n      console.log(`🧹 Normal cleanup: removed ${toRemove.length} timed out connections`);\n    }\n  }\n  \n  // Start cleanup interval\n  startCleanup() {\n    this.cleanupInterval = setInterval(() => {\n      this.performNormalCleanup();\n      \n      // Check memory usage\n      const memoryStatus = memoryMonitor.getMemoryStatus();\n      if (memoryStatus.usagePercentage > 80) {\n        this.handleMemoryPressure('emergency');\n      }\n      \n    }, this.CLEANUP_INTERVAL);\n    \n    // Register with memory monitor\n    memoryMonitor.registerCleanupCallback((type) => {\n      this.handleMemoryPressure(type);\n    });\n    \n    console.log('⏰ Cleanup interval started');\n  }\n  \n  // Get current state (memory-efficient)\n  getState() {\n    return {\n      alerts: this.displayState.alerts.slice(-50), // Limit alerts returned\n      lastUpdated: this.displayState.lastUpdated,\n      connections: {\n        total: this.connections.size,\n        supervisors: Array.from(this.connections.values())\n          .filter(c => c.type === 'supervisor').length,\n        displays: Array.from(this.connections.values())\n          .filter(c => c.type === 'display').length\n      },\n      memoryPressureMode: this.memoryPressureMode\n    };\n  }\n  \n  // Get statistics\n  getStats() {\n    const now = Date.now();\n    let totalMessages = 0;\n    let activeConnections = 0;\n    \n    for (const connection of this.connections.values()) {\n      totalMessages += connection.messageCount;\n      if (now - connection.lastActivity < 60000) {\n        activeConnections++;\n      }\n    }\n    \n    return {\n      connections: {\n        total: this.connections.size,\n        active: activeConnections,\n        maxAllowed: this.MAX_CONNECTIONS\n      },\n      ipConnections: Object.fromEntries(this.ipConnections),\n      messages: {\n        total: totalMessages,\n        averagePerConnection: this.connections.size > 0 ? \n          Math.round(totalMessages / this.connections.size) : 0\n      },\n      state: {\n        alerts: this.displayState.alerts.length,\n        historySize: this.stateHistory.length,\n        lastUpdated: this.displayState.lastUpdated\n      },\n      memory: {\n        pressureMode: this.memoryPressureMode,\n        estimatedUsage: this.getEstimatedMemoryUsage()\n      }\n    };\n  }\n  \n  // Estimate memory usage\n  getEstimatedMemoryUsage() {\n    let estimatedKB = 0;\n    \n    // Estimate connection storage\n    estimatedKB += this.connections.size * 0.5; // ~0.5KB per connection\n    \n    // Estimate display state\n    estimatedKB += this.displayState.alerts.length * 2; // ~2KB per alert\n    \n    // Estimate state history\n    estimatedKB += this.stateHistory.length * 0.1; // ~0.1KB per history entry\n    \n    return Math.round(estimatedKB / 1024 * 100) / 100; // Convert to MB\n  }\n  \n  // Shutdown\n  shutdown() {\n    console.log('🏁 Memory-optimized Supervisor Sync shutting down...');\n    \n    // Clear cleanup interval\n    if (this.cleanupInterval) {\n      clearInterval(this.cleanupInterval);\n    }\n    \n    // Remove all connections\n    this.connections.clear();\n    this.ipConnections.clear();\n    this.lastActivity.clear();\n    \n    // Clear state\n    this.displayState.alerts = [];\n    this.stateHistory = [];\n    \n    console.log('✅ Memory-optimized Supervisor Sync shutdown complete');\n  }\n}\n\n// Create singleton instance\nconst memoryOptimizedSupervisorSync = new MemoryOptimizedSupervisorSync();\n\nexport default memoryOptimizedSupervisorSync;